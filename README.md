# 📡 局域网双向传输工具

一款简洁高效的局域网文件传输工具，支持电脑与手机、电脑与电脑之间的实时消息和文件互传。

## ✨ 功能特性

- 📱 **跨设备通信**：支持电脑与手机、电脑与电脑之间的双向消息和文件传输
- 🌐 **局域网传输**：仅在本地局域网内传输数据，无需互联网
- 📤 **大文件支持**：支持大文件高速传输，带实时进度条显示
- 📁 **文件夹传输**：自动压缩后发送文件夹，保留目录结构
- 🔄 **实时消息**：支持文字消息的实时发送和接收
- 🖥️ **PC 发现**：自动发现局域网中的其他 PC 设备，一键连接
- 📲 **扫码连接**：手机扫码即可快速连接，无需手动输入地址
- 🔒 **隐私安全**：不上传任何数据到远程服务器，保护用户隐私

## 🚀 快速开始

### 前置条件

运行前请确保已安装以下依赖：

```bash
pip install flask flask-socketio gevent qrcode Pillow requests PyQt5
```

或者使用 requirements.txt：

```bash
pip install -r requirements.txt
```

### 运行方式

```bash
# 直接运行
python main.py

# 可选：指定下载路径
python main.py --download-path D:/Downloads
```

### 使用步骤

1. **启动程序**：运行 `main.py`，程序会自动启动本地服务器
2. **连接手机**：
   - 确保手机和电脑连接到同一 Wi-Fi 网络
   - 打开手机浏览器，扫描界面上的二维码
   - 或手动访问显示的服务器地址
3. **PC 互联**：
   - 程序会自动发现同一局域网中的其他 PC 设备
   - 在左侧设备列表中双击 PC 设备即可建立连接
4. **开始传输**：
   - 文字消息：在输入框中输入文字，按回车或点击发送按钮
   - 文件传输：点击文件按钮选择要发送的文件或文件夹

## 📁 文件结构

main.py              # 程序入口
config.json          # 配置文件（自动生成）
requirements.txt     # 依赖清单
LanFastTransfer.spec # PyInstaller 打包配置

app/                 # 核心模块
├── __init__.py
├── server.py        # Flask 服务器 + SocketIO + 手机端网页
├── config.py        # 配置文件读写管理
├── signal.py        # Qt 信号定义
├── utils.py         # 工具函数（IP 获取、文件大小格式化等）
├── discovery.py     # UDP 广播设备发现
├── pc_client.py     # PC 客户端连接模块
└── ui/
    ├── __init__.py
    ├── main_window.py       # 主窗口界面
    └── device_selector.py   # 设备选择对话框

## 🛠️ 技术栈

- **后端**: Python 3 + Flask + Flask-SocketIO + Gevent
- **前端**: HTML5 + CSS3 + JavaScript（手机端）
- **桌面界面**: PyQt5
- **二维码**: qrcode + Pillow
- **设备发现**: UDP 广播

## 📱 手机端功能

手机端网页支持：
- 文字消息发送和接收
- 文件上传（带进度条）
- 文件夹上传（保留目录结构）
- 文件下载
- 连接状态实时显示
- 关于页面（含隐私声明和免责声明）

## 🖥️ PC 互联功能

- 自动发现局域网中的 PC 设备（UDP 广播）
- 设备列表分组显示（手机 / PC）
- PC 间文字消息和文件互传
- 多 IP 选择（支持多网卡环境）

## 🔒 隐私与安全

**重要声明：**

- ✅ 本软件仅在本地局域网内传输数据
- ✅ 不上传任何数据到远程服务器
- ✅ 不收集、不窃取用户隐私信息
- ✅ 不监控、不记录用户剪贴板内容
- ✅ 无后台运行、无隐蔽数据传输

## ⚠️ 免责声明

本软件为个人免费工具，仅供个人非商业用途使用。

作者不对使用本软件造成的任何直接或间接损失负责。使用本软件即表示您同意上述条款。

## 📧 联系与反馈

如有问题或建议，欢迎反馈！您的反馈是我们改进的动力。

## 📜 许可证

MIT License

## 📝 更新日志

### v2.0
- 重构为模块化项目结构（app/ 包）
- 新增 PC 间设备发现和连接（UDP 广播）
- 新增 PC 间文字消息和文件传输
- 新增文件夹传输（自动压缩为 ZIP）
- 新增设备列表分组显示（手机 / PC）
- 新增聊天历史记录（按设备独立存储）
- 新增多 IP 选择对话框
- 新增折叠式服务器信息面板
- 新增下载路径配置和持久化
- 新增关于页面（含隐私声明和免责声明）
- 手机端新增文件夹上传功能
- 手机端新增弹出式菜单操作
- 手机端文件上传增加进度条显示
- 改进整体界面样式和交互体验
- 支持 PyInstaller 打包为独立可执行文件

### v1.0
- 基础功能实现
- 电脑与手机双向消息传输
- 文件传输功能
- 二维码连接功能

---
----------------

🏢 原平沐阳网络 · 开源实验室  
📬 更多效率工具关注公众号：**沐阳网络干货**